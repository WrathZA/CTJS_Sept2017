let arr = [1,2,3,4,5,6];

const reducer = function(accumulator, current) { //SUM!
  return accumulator + current;
}

let ret = arr.reduce(reducer, 0); //Array.prototype.reduce()

console.log(ret); //21
